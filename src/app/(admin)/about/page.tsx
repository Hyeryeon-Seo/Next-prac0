import React from "react";

const AdminAboutPage = () => {
	return (
		<div>
			<p>
				admin 폴더 about 폴더에 있지만, (admin) 폴더라서 routing에 포함되지
				않아. 그저 폴더 구분을 위한 것!
			</p>
			<p>이 페이지에 오려면 localhost:3000/about 경로로 들어와야 해</p>
		</div>
	);
};

export default AdminAboutPage;
