import React from "react";

const BusinessPage = () => {
	return (
		<div>
			`This is Business Page!` &nbsp; /business 로 올 수 있는 페이지야.
			(marketing) 폴더는 routing에 포함되지 않는다구 !
		</div>
	);
};

export default BusinessPage;
